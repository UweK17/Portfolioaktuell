{
  "name": "portfolioaktuell",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "make": "mkdir sass && mkdir css && touch sass/style.scss && mkdir script && touch script/index.js && touch index.html && touch .gitignore && echo node_modules/ > .gitignore",
    "watch": "sass sass:css --watch",
    "live": "live-server",
    "dev": "npm-run-all --parallel watch live"
  },
  "keywords": [],
  "author": "UK",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "sass": "^1.52.2"
  }
}
